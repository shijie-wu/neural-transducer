To run the experiments, the following environment variable needs to be set in `{tags,notags}-{mono,trm}.sh`.

- `gpu`: set to GPU id
- `data_dir`: where the data is stored
- `ckpt_dir`: where the model is saved